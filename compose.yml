services:
  projectflow:
    build:
      context: .
      dockerfile: Dockerfile
    image: projectflow-edu:local
    container_name: projectflow-edu-local
    ports:
      - "8080:8080"
    environment:
      APP_ENV: local
      WORKSPACE_ID: test-user
      USE_IN_MEMORY_REPOSITORY: "true"
